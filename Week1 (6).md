# Excel 1주차 정규 과제 

📌Excel 정규과제는 매주 정해진 분량의 『*진짜 쓰는 실무 엑셀*』 을 읽고 학습하는 것입니다. 이번주는 아래의 **EXCEL_1st_TIL**에 나열된 분량을 읽고 공부하시면 됩니다.

아래의 문제를 풀어보며 학습 내용을 점검하세요. 문제를 해결하는 과정에서 개념을 스스로 정리하고, 필요한 경우 제시된 강의를 참고하여 보완하는 것이 좋습니다.

**교재 실습 예제 파일은 1주차 과제 설명 노션에 있습니다. 해당 파일을 다운 후 실습 진행해주시면 됩니다.**

**👀(수행 인증샷은 필수입니다.)** 

## EXCEL_1st_TIL

### 1장 시작부터 남다른 실무자의 엑셀 활용
#### 01. 엑셀 기본 설정 변경으로 맞춤 환경 만들기 (범위 제외)
#### 02. 엑셀 좀 사용하려면 반드시 알아야 할 엑셀 기본키 (범위 제외)
#### 03. 엑셀의 기본 동작 원리 이해하기
#### 04. 엑셀에서 발생하는 모든 오류와 해결 방법 총정리
#### 05. 실무자면 꼭 알아야 할, 필수 단축키 모음

### 2장 실무자라면 반드시 알아야 할 엑셀 활용
#### 01. 보기 좋은 표, 활용할 수 있는 데이터를 위한 필수 상식
#### 02. 편리한 엑셀 문서 작업을 위한 실력 다지기
#### 03. 엑셀로 시작하는 기초 데이터 분석
#### 02. 엑셀 데이터 가공을 위한 텍스트 나누고 합치기


## Study Schedule

| 주차  | 공부 범위     | 완료 여부 |
| ----- | ------------- | --------- |
| 1주차 | p.22~111    | ✅         |
| 2주차 | p.112~157   | 🍽️         |
| 3주차 | p.158~213  | 🍽️         |
| 4주차 | p.214~273 | 🍽️         |
| 5주차 | p.274~339 | 🍽️         |
| 6주차 | p.340~425 | 🍽️         |
| 7주차 | p.426~472 | 🍽️         |


<br>

<!-- 여기까진 그대로 둬 주세요-->

---

# 1️⃣ 학습 내용 정리

### 셀 참조 방식이 중요한 이유
하나의 셀에서만 결과를 구한다면 참조 방식은 중요하지 않다. 하지만 **수식을 복사하거나 자동 채우기를 실행하는 순간**, 참조 방식에 따라 결과가 완전히 달라진다. 그래서 함수의 형식을 지키는 것만큼이나 참조 방식을 이해하는 것이 중요하다.

### 핵심 원리 — `$` 기호
- `$` 기호는 **행/열을 고정할 때** 사용한다.
- 행과 열을 **모두 고정**하거나 **둘 중 하나만** 고정할 수 있다.
- `$` 사용 여부에 따라 **상대 참조 / 절대 참조 / 혼합 참조**로 구분된다.
- 셀 주소에 직접 `$`를 입력하거나, 셀 주소를 선택한 후 <kbd>F4</kbd>를 반복해서 누르면 참조 방식이 순환한다.

### 3가지 참조 방식

| 참조 방식 | 표기 예시 | 고정 대상 | 복사·자동 채우기 시 동작 |
| --- | --- | --- | --- |
| **상대 참조** | `=A1`, `=C6+D6` | 고정 없음 | 복사한 위치만큼 행·열 주소가 **함께 바뀜** |
| **절대 참조** | `=$A$1`, `=$F$5` | 열 + 행 모두 | 어디로 복사해도 주소가 **항상 고정** |
| **혼합 참조(행 고정)** | `=A$1`, `=C$4` | 행(숫자)만 | 열은 바뀌고 **행은 고정** |
| **혼합 참조(열 고정)** | `=$A1`, `=$B5` | 열(알파벳)만 | 행은 바뀌고 **열은 고정** |

### <kbd>F4</kbd> 순환 순서
```
A3 (상대 참조)
  ↓ F4
$A$3 (절대 참조)
  ↓ F4
A$3 (혼합 참조 - 행 고정)
  ↓ F4
$A3 (혼합 참조 - 열 고정)
  ↓ F4
A3 (다시 상대 참조)
```

### 교재 예제로 이해하기
- **상대 참조**: `[E6]`셀에 `=C6+D6`을 입력한 후 아래로 자동 채우기 → `=C7+D7`, `=C8+D8`처럼 참조한 셀 주소도 함께 바뀐다.
- **절대 참조**: 커미션 비율이 들어 있는 `[F5]`셀은 항상 같은 셀을 봐야 하므로 `[F6]`셀에 `=$F$5*E6`을 입력한다. 자동 채우기를 해도 절대 참조 부분(`$F$5`)은 고정된 채 상대 참조 부분(`E6`)만 바뀐다.
- **혼합 참조**: 판매량 × 커미션 비율 표(구구단 형태)처럼 **오른쪽과 아래 두 방향으로 동시에** 자동 채우기를 해야 할 때 사용한다. 커미션 비율은 `C4→D4→E4→F4`로 **행이 고정**되어야 하고, 판매량은 `B5→B6→B7→B8`로 **열이 고정**되어야 하므로 `[C5]`셀에 `=C$4*$B5`를 입력한다.

> 💡 **정리**: "복사·자동 채우기를 했을 때 같이 따라와야 하는가?"를 기준으로 판단한다. 따라와야 하면 상대 참조, 절대 움직이면 안 되면 절대 참조, 한 방향으로만 따라와야 하면 혼합 참조를 쓴다.


## 01-4. 엑셀에서 발생하는 모든 오류와 해결 방법 총정리
> **숫자처럼 보이는 문자를 빠르게 확인하고 해결하기(51 ~52p)를 진행 후 인증사진을 첨부해주세요.**

**실습 요약 (01-005.xlsx)**
1. **찾기**: `[발주서 예제]` 시트에서 수량이 입력된 `[F12:F17]` 범위 선택 → <kbd>F5</kbd>(또는 [홈] 탭-[편집] 그룹-[찾기 및 선택]-[이동 옵션]) → **[상수]** 선택 후 **[텍스트]**만 체크 → [확인] → 숫자처럼 보이는 문자만 선택된다.
   - 셀 값이 숫자처럼 보이지만 문자 형태이면 셀 왼쪽 위에 **초록색 삼각형**이 표시된다.
2. **해결(선택하여 붙여넣기)**: 빈 셀에 `1`을 입력하고 <kbd>Ctrl</kbd>+<kbd>C</kbd> → 위에서 찾은 범위에 마우스 우클릭 → [선택하여 붙여넣기] → **[값]** + **[곱하기]** 선택 → [확인] → 1을 곱하면서 숫자 형식으로 바뀐다. (※ [모두]로 붙여넣으면 서식이 망가지므로 반드시 [값] 선택)
3. **해결(오류 검사)**: [수식] 탭-[수식 분석] 그룹-[오류 검사] → 오류 위치를 찾아주면 **[숫자로 변환]** 클릭.

📸 **인증샷**

<!-- 이 부분을 지우고 인증사진을 제출해주세요.-->


## 01-5. 실무자면 꼭 알아야 할, 필수 단축키 모음
> **이번에 배운 단축키에 대해 정리해주세요.**

### 0. Ctrl 조합 vs Alt 조합
| 구분 | 누르는 방법 | 특징 |
| --- | --- | --- |
| **Ctrl 조합** | **동시에** 누름 (예: <kbd>Ctrl</kbd>+<kbd>1</kbd>) | 빠르지만 **제한적인 기능**에서만 사용 가능 |
| **Alt 조합** | **차례대로** 누름 (예: <kbd>Alt</kbd>→<kbd>P</kbd>→<kbd>R</kbd>→<kbd>S</kbd>) | 리본 메뉴의 **거의 모든 기능** 실행 가능 / <kbd>Alt</kbd>를 누르면 화면에 단축키가 표시되므로 **외우지 않아도 됨** |

### 1. 엑셀 파일 관리 단축키
| 단축키 | 기능 |
| --- | --- |
| <kbd>Ctrl</kbd>+<kbd>S</kbd> | 파일 저장 (작업 중 수시로 눌러 데이터 손실 방지) |
| <kbd>F12</kbd> | 다른 이름으로 저장 (버전별 파일 관리에 유용) |
| <kbd>Shift</kbd>+<kbd>F11</kbd> | 현재 시트 왼쪽에 새 시트 삽입 |
| <kbd>Ctrl</kbd>+<kbd>N</kbd> | 새 통합 문서 만들기 |
| <kbd>Ctrl</kbd>+<kbd>Tab</kbd> | 통합 문서 간 이동 |
| <kbd>Alt</kbd>+<kbd>Tab</kbd> | 실행 중인 모든 프로그램 간 이동 |
| <kbd>Ctrl</kbd>+<kbd>PageUp</kbd> / <kbd>PageDown</kbd> | 이전 / 다음 시트로 이동 (길게 누르면 첫 시트·마지막 시트) |
| <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>PageUp</kbd> / <kbd>PageDown</kbd> | 연속된 시트 다중 선택 |
| <kbd>Ctrl</kbd>+마우스 휠 | 화면 배율 조절 (<kbd>Ctrl</kbd>+<kbd>Alt</kbd>+<kbd>+</kbd>/<kbd>-</kbd>도 가능) |
| <kbd>Alt</kbd>→<kbd>P</kbd>→<kbd>R</kbd>→<kbd>S</kbd> | 인쇄 영역 설정 (설정 후 <kbd>Ctrl</kbd>+<kbd>P</kbd>로 미리 보기 확인) |

### 2. 보다 편리한 엑셀 작업을 위한 단축키
| 단축키 | 기능 |
| --- | --- |
| <kbd>Shift</kbd>+<kbd>F10</kbd> | 팝업 메뉴(마우스 우클릭 메뉴) 표시 → 방향키+<kbd>Enter</kbd>로 실행 |
| <kbd>Ctrl</kbd>+<kbd>F1</kbd> | 리본 메뉴 숨기기 / 표시 |
| <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>F1</kbd> | 전체화면 모드 전환 |
| <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>U</kbd> | 수식 입력줄 확장 / 축소 (긴 수식 확인용) |
| <kbd>Alt</kbd>→<kbd>W</kbd>→<kbd>F</kbd>→<kbd>F</kbd> | 행/열 틀 고정 |
| <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>L</kbd> | 자동 필터 적용 (이후 <kbd>Alt</kbd>+<kbd>↓</kbd>→<kbd>E</kbd>로 검색창 이동) |
| <kbd>Ctrl</kbd>+<kbd>\\</kbd> | 기준 행과 값이 다른 셀 선택 (**좌우** 비교) |
| <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>\\</kbd> | 기준 열과 값이 다른 셀 선택 (**상하** 비교) |
| <kbd>Ctrl</kbd>+<kbd>[</kbd> | 현재 셀의 수식이 **참조하는** 셀(사용한 셀)로 이동 |
| <kbd>Ctrl</kbd>+<kbd>]</kbd> | 현재 셀을 **참조하는** 셀(사용된 셀) 선택 |

### 3. 자료 편집 작업을 빠르게 하는 단축키
| 단축키 | 기능 |
| --- | --- |
| <kbd>Ctrl</kbd>+<kbd>A</kbd> | 연속된 범위 선택 (한 번 더 누르면 시트 전체) |
| <kbd>Alt</kbd>+<kbd>Enter</kbd> | 셀 안에서 줄 바꿈 |
| <kbd>Ctrl</kbd>+<kbd>C</kbd> / <kbd>Ctrl</kbd>+<kbd>V</kbd> | 복사 / 붙여넣기 |
| <kbd>Ctrl</kbd>+<kbd>Alt</kbd>+<kbd>V</kbd> | **선택하여 붙여넣기** 대화상자 (값만, 행/열 바꿈 등) |
| <kbd>Ctrl</kbd>+<kbd>F</kbd> / <kbd>Ctrl</kbd>+<kbd>H</kbd> | 찾기 / 바꾸기 |
| <kbd>F4</kbd> | **이전 작업 반복** (배경색 변경, 셀 병합 등 단축키가 없는 작업에 특히 유용) |
| <kbd>F2</kbd> | 활성화된 셀 즉시 편집 모드 (방향키로 셀 참조가 바뀌는 문제 방지) |
| <kbd>Ctrl</kbd>+<kbd>Z</kbd> / <kbd>Ctrl</kbd>+<kbd>Y</kbd> | 실행 취소 / 다시 실행 |
| <kbd>Ctrl</kbd>+<kbd>Enter</kbd> | 선택한 범위 **모든 셀에 값·수식 한 번에 입력** (떨어진 범위도 가능) |
| <kbd>Ctrl</kbd>+<kbd>+</kbd> / <kbd>Ctrl</kbd>+<kbd>-</kbd> | 행·열·셀 삽입 / 삭제 (키패드 사용, 없으면 <kbd>Shift</kbd> 함께) |
| <kbd>Ctrl</kbd>+<kbd>D</kbd> / <kbd>Ctrl</kbd>+<kbd>R</kbd> | 아래쪽 / 오른쪽 방향 자동 채우기 (패턴은 인식하지 않고 첫 셀 값으로만 채움) |
| <kbd>Ctrl</kbd>+<kbd>E</kbd> | 빠른 채우기 |

### 4. 셀 서식을 빠르게 변경하는 단축키
| 단축키 | 기능 |
| --- | --- |
| <kbd>Ctrl</kbd>+<kbd>1</kbd> | **셀 서식 대화상자 열기** (반드시 기억할 단축키) |
| <kbd>Alt</kbd>→<kbd>H</kbd>→<kbd>B</kbd>→<kbd>A</kbd> | 선택 범위 안쪽+바깥쪽 기본 테두리 적용 |
| <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>7</kbd> | 바깥쪽 테두리만 적용 |
| <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>-</kbd> | 적용된 모든 테두리 지우기 |
| <kbd>Alt</kbd>→<kbd>H</kbd>→<kbd>H</kbd>→<kbd>N</kbd> | 채우기 색상 지우기 |

**표시 형식 단축키 7가지**

| 단축키 | 적용 서식 | 예시 |
| --- | --- | --- |
| <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>`</kbd> | 일반 서식 | 1000 |
| <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>1</kbd> | 숫자 서식 | 1000 → 1,000 |
| <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>2</kbd> | 시간 서식 | 08:30, 09:15 |
| <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>3</kbd> | 날짜 서식 | 2020-03-09 |
| <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>4</kbd> | 통화 서식 | 1000 → ₩1,000 |
| <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>5</kbd> | 백분율 서식 | 0.1 → 10% |
| <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>6</kbd> | 지수 서식 | 1000 → 1.00E+03 |

### 5. 셀 이동 및 선택이 빨라지는 단축키
| 단축키 | 기능 |
| --- | --- |
| <kbd>Ctrl</kbd>+방향키 | 연속된 데이터의 **마지막 셀로 이동** |
| <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+방향키 | 연속된 데이터 **끝까지 한 번에 선택** |
| <kbd>Ctrl</kbd>+<kbd>Backspace</kbd> | 현재 활성화된 셀로 화면 이동 |
| <kbd>Ctrl</kbd>+<kbd>Home</kbd> / <kbd>Ctrl</kbd>+<kbd>End</kbd> | `[A1]`셀 / 사용된 범위의 마지막 셀로 이동 |
| <kbd>F5</kbd> 또는 <kbd>Ctrl</kbd>+<kbd>G</kbd> | **이동 대화상자** ([옵션] → 이동 옵션) |
| <kbd>Ctrl</kbd>+<kbd>Space</kbd> / <kbd>Shift</kbd>+<kbd>Space</kbd> | 열 전체 / 행 전체 선택 (<kbd>Shift</kbd>는 가로로 길어서 행, <kbd>Ctrl</kbd>은 열로 암기) |
| <kbd>Alt</kbd>+<kbd>;</kbd> | 숨겨진 부분을 제외하고 **보이는 데이터만 선택** |
| <kbd>Alt</kbd>+<kbd>Shift</kbd>+<kbd>→</kbd> / <kbd>←</kbd> | 범위 그룹화 / 그룹 해제 |
| <kbd>Ctrl</kbd>+<kbd>9</kbd> / <kbd>Ctrl</kbd>+<kbd>0</kbd> | 행 숨기기 / 열 숨기기 (해제는 <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>9</kbd> / <kbd>0</kbd>) |

**💡 자주 쓰는 [이동 옵션] 4가지**
| 옵션 | 활용 |
| --- | --- |
| **메모** | 메모가 표시된 셀로 빠르게 이동 |
| **상수 + 텍스트** | 숫자처럼 보이는 문자 찾기 |
| **수식 + 오류** | 오류가 발생한 셀로 이동 |
| **빈 셀** | 표 안의 빈 셀 한 번에 찾기 |



## 02-1. 보기 좋은 표, 활용할 수 있는 데이터를 위한 필수 상식
> **데이터와 표를 비교해주세요**

표와 데이터는 겉으로 보기에는 비슷하지만 **목적과 구조가 완전히 다르다.** 교재에서는 "표와 데이터의 차이만 확실하게 이해해도 업무량을 90% 이상 단축시킬 수 있다"고 강조한다.

| 구분 | **표(서식)** | **데이터** |
| --- | --- | --- |
| 정의 | 사람이 **보기 편하도록 시각화된 서식** (차트·그래프와 마찬가지로 시각화의 한 종류) | 엑셀이 **처리하기 좋은 형태**로 정리된 원본 자료 |
| 구조 | 항목이 가로·세로로 흩어져 있음 (이름은 세로, 월은 가로) | **세로 방향으로 차곡차곡 블록 쌓기**, 한 행 = 한 건의 기록 |
| 목적 | 보고·전달 (가독성) | 정렬·필터·함수·피벗 테이블 등 **가공과 분석** |
| 데이터 누적 | 가로 방향으로 계속 넓어지고 **빈칸이 발생** | 아래로 행만 추가하면 됨 |
| 구분자 추가 | '아파트/주택' 같은 항목이 추가되면 **레이아웃 전체를 다시 수정**해야 함 | 열 하나만 추가하면 끝 |
| 정렬·최댓값 찾기 | MAX·LARGE 등 **별도 수식을 써야** 하고 번거로움 | [데이터]-[정렬 및 필터]로 **바로 해결** |
| 피벗 테이블 | 사실상 활용 불가 | 그대로 피벗 테이블 소스로 사용 가능 |

**핵심 비유 — "데이터 관리는 세로 방향 블록 쌓기"**
블록을 쌓다가 한쪽으로 무게 중심이 쏠리면 무너지듯이, 데이터도 각 항목을 하나의 블록이라 생각하고 **세로 방향으로 차곡차곡 쌓아야** 한다.

> ✅ **꼭 명심!** 함수를 잘 사용하는 것보다 **올바른 데이터 관리가 더 중요하다.** 데이터만 잘 관리되어 있으면 고급 수식이나 VBA 없이도, 간단한 함수와 기능만으로 실무에서 발생하는 대부분의 문제를 해결할 수 있다.

<br>

> **셀 병합 기능을 자제해야 하는 이유를 정리해주세요.**

셀 병합은 **엑셀로 깔끔한 서식을 제작할 때 반드시 필요한 기능**이지만, 무분별하게 사용하면 여러 문제를 일으킨다. 그래서 교재에서는 엑셀 사용이 익숙하지 않은 사용자에게는 "셀 병합 기능은 절대 사용하지 마세요"라고 안내한다.

**가장 근본적인 원인**: 셀을 병합하면 **병합된 셀 중 첫 번째 셀의 값만 인식**된다.
> 예) 병합된 범위의 합계를 구하려고 `[J5]`셀에 `=SUM(G5:I5)`를 입력하고 `[J8]`셀까지 자동 채우기를 하면, `[J6]`셀의 값이 의도와 달리 **1로 계산**된다.

**셀 병합으로 발생하는 대표적인 문제 6가지**
1. **범위 선택이 제한**된다. (`[D4:E10]`을 드래그하면 의도치 않게 `[F]`열까지 선택됨)
2. **범위의 수정/편집이 제한**된다. (잘라내기 <kbd>Ctrl</kbd>+<kbd>X</kbd> 실행 시 오류 메시지 발생)
3. **표 기능과 피벗 테이블 사용이 제한**된다.
4. **자동 채우기를 제대로 활용할 수 없다.**
5. **데이터 정렬 기능을 사용할 수 없다.**
6. **함수 사용 시 옳지 않은 결과를 반환**할 수 있다.

> 정리: 셀 병합은 보기에 깔끔할 수는 있으나 엑셀의 기능을 제대로 활용하는 데는 많은 제약이 따른다. 가운데 정렬이 목적이라면 **[셀 서식]-[맞춤]-[선택 영역의 가운데로]** 를 사용하고, 단순히 가리는 것이 목적이라면 **숨기기 대신 [그룹] 기능**을 사용하는 것이 좋다. (숨기기는 다른 사용자가 숨겨진 범위를 다시 찾아야 하는 번거로움이 생긴다.)


## 02-2. 편리한 엑셀 문서 작업을 위한 실력 다지기
> **셀 병합하지 않고 가운데 정렬하(75 ~77p)를 진행 후 인증사진을 첨부해주세요.**
<img width="1637" height="625" alt="image" src="https://github.com/user-attachments/assets/fe7d8413-26f6-4f5e-9a61-875c5018cb9a" />


> **셀 병합 해제 후 빈칸 쉽게 체우기(78 ~79p)를 진행 후 인증사진을 첨부해주세요.**
<img width="615" height="590" alt="image" src="https://github.com/user-attachments/assets/c219333d-bf90-4bbf-8c61-349334070674" />


> **빈 셀을 한 번에 찾고 내용 입력하기(80 ~81p)를 진행 후 인증사진을 첨부해주세요.**
<img width="747" height="641" alt="image" src="https://github.com/user-attachments/assets/13765cbf-59ed-410a-bd95-aef73749c72d" />


> **숫자 데이터의 기본 단위를 한 번에 바꾸는 방법(84 ~87p)를 진행 후 인증사진을 첨부해주세요.**
<img width="1292" height="636" alt="image" src="https://github.com/user-attachments/assets/929f846e-42ec-4057-a91e-60d5a9cba1c5" />



## 02-3. 엑셀로 시작하는 기초 데이터 분석
> **행/열 전환하여 새로운 관점으로 데이터 살펴보기(88 ~90p)를 진행 후 인증사진을 첨부해주세요.**
<img width="1732" height="507" alt="image" src="https://github.com/user-attachments/assets/cca80aec-3aac-4193-8b7b-de8c844d8efc" />


> **중복된 데이터 입력 제한하기(90 ~92p)를 진행 후 인증사진을 첨부해주세요.**
<img width="1482" height="641" alt="image" src="https://github.com/user-attachments/assets/1304908f-4ff8-4548-8b75-4123d68c4a82" />



## 02-4. 엑셀 데이터 가공을 위한 텍스트 나누고 합치기
> **여러 줄을 한 줄로 합치거나 한 줄을 여러 줄로 분리하기(104 ~107p)를 진행 후 인증사진을 첨부해주세요.**
<img width="1111" height="557" alt="image" src="https://github.com/user-attachments/assets/1d056f27-b989-494e-8eba-e20b91b60cc1" />


> **여러 열에 입력된 내용을 간단하게 한 열로 합치기(108 ~109p)를 진행 후 인증사진을 첨부해주세요.**
<img width="1296" height="562" alt="image" src="https://github.com/user-attachments/assets/8b01a15c-8114-462c-94b8-d64f62bee4ea" />


---


### 🎉 수고하셨습니다.







